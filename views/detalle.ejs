<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalle</title>
</head>
<body>
    <head>
        <%- include('partials/head') %>
    </head>
    <main>
        <h1><%= pelicula.title %></h1>
        <img src="<%= pelicula.image %>" alt="<%= pelicula.title %>" style="width: 800px; height: 400px;">

        <p><%= pelicula.intro %></p>

        <p><%= pelicula.category %></p>

    </main>
    <footer>
        <%- include('partials/footer') %>
    </footer>
</body>
</html>
